# Node Strategy

Each installation is a node with independent operation.

Milestone 3 scope:
- Node identity and authenticated private API.
- Health-aware node routing with local fallback.
- Optional remote inference forwarding over private network.
